# Server Configuration
PORT=3001

# Anthropic API Configuration
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# AI Model Selection
# Options: "haiku" (default, cost-efficient) or "sonnet" (higher quality)
# Haiku is used by default for all requests
# If Haiku fails or produces invalid responses, Sonnet is automatically used as rescue
AI_MODEL=haiku

# Bug Report Email Configuration
# The email address where bug reports will be sent
BUG_REPORT_EMAIL=growzone.ai@gmail.com

# The "from" address for bug report emails
BUG_REPORT_FROM=deskai-bugs@yourdomain.com

# SMTP Server Configuration
# For Gmail: smtp.gmail.com (requires app-specific password)
# For SendGrid: smtp.sendgrid.net (use API key as password)
# For MailerSend, Resend, etc.: check their SMTP settings
BUG_REPORT_SMTP_HOST=smtp.gmail.com
BUG_REPORT_SMTP_PORT=587
BUG_REPORT_SMTP_USER=your_email@gmail.com
BUG_REPORT_SMTP_PASS=your_app_specific_password_here

# Note: If SMTP is not configured, bug reports will be logged to console instead

# ============================================================================
# Google Calendar Integration (OPTIONAL)
# ============================================================================
# Set to "true" to enable Google Calendar sync for appointments
# When enabled, appointments with scheduledStart will be added to Google Calendar
# When disabled, appointments are only stored locally (safe for development)
GOOGLE_CALENDAR_ENABLED=false

# Google Calendar ID - usually "primary" for your main calendar
# Or use a specific calendar ID like "abc123@group.calendar.google.com"
GOOGLE_CALENDAR_ID=primary

# Google Service Account Credentials
# Create a service account at https://console.cloud.google.com/
# Enable Google Calendar API
# Create and download JSON key file
# Share your calendar with the service account email

# Service account email (found in your JSON key file)
GOOGLE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Service account private key (found in your JSON key file)
# Copy the entire "private_key" value including the -----BEGIN/END PRIVATE KEY----- lines
# Replace actual newlines with \n in the .env file
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# SETUP INSTRUCTIONS:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google Calendar API
# 4. Create Service Account (IAM & Admin > Service Accounts)
# 5. Download JSON key file
# 6. Open Google Calendar settings
# 7. Share your target calendar with the service account email
# 8. Give it "Make changes to events" permission
# 9. Copy credentials from JSON to this .env file
# 10. Set GOOGLE_CALENDAR_ENABLED=true

